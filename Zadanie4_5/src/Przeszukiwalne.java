public interface Przeszukiwalne {
    boolean czyPasuje(String wzorzec);
    // co to jest interface i co zawiera

    // Interface to klasa abstrakcyjna, ktorej uzywa sie do grupowania metod klasy z pustym cialem
}
